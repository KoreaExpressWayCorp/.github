();S.img=img;img.onload=()=>{setCanvas(img.naturalWidth,img.naturalHeight);cc.drawImage(img,0,0,clean.width,clean.height);drawBase()};img.onerror=()=>toast('이미지를 열 수 없습니다.','err');img.src=S.url}else{els.video.src=S.url;els.video.muted=true;await new Promise((res,rej)=>{els.video.onloadedmetadata=res;els.video.onerror=rej}).catch(()=>toast('브라우저에서 영상을 열 수 없습니다.','err'));if(!els.video.videoWidth)return;setCanvas(els.video.videoWidth,els.video.videoHeight);els.video.currentTime=Math.min(.05,els.video.duration||0);await new Promise(res=>{els.video.onseeked=res;setTimeout(res,400)});frame();els.videoBar.style.display='grid';els.time.textContent=`00:00 / ${clock(els.video.duration)}`;els.seek.value=0}els.process.disabled=false;toast('파일을 불러왔습니다. 미리보기에서 보호 영역을 지정하세요.')}
function frame(){if(S.kind!=='video'||!els.video.videoWidth)return;cc.clearRect(0,0,clean.width,clean.height);cc.drawImage(els.video,0,0,clean.width,clean.height);drawBase();els.seek.value=els.video.duration?els.video.currentTime/els.video.duration*1000:0;els.time.textContent=`${clock(els.video.currentTime)} / ${clock(els.video.duration)}`;if(!els.video.paused&&!els.video.ended)requestAnimationFrame(frame)}
function reset(scroll=true){if(S.url)URL.revokeObjectURL(S.url);if(S.resultUrl)URL.revokeObjectURL(S.resultUrl);els.video.pause();els.video.removeAttribute('src');S={...S,file:null,kind:null,url:null,img:null,regions:[],draft:null,start:null,result:null,resultUrl:null,manifest:null,playing:false};els.file.value='';els.drop.style.display='flex';els.preview.style.display='none';els.videoBar.style.display='none';els.progress.style.display='none';els.result.style.display='none';els.resultImg.style.display='none';els.resultVideo.style.display='none';els.process.disabled=true;clean.width=clean.height=els.canvas.width=els.canvas.height=0;if(scroll)$('#workspace').scrollIntoView({behavior:'smooth'})}
function mask(target,base,r){const m=$('#mode').value,k=Number($('#strength').value);target.save();if(m==='black'){target.fillStyle='#050505';target.fillRect(r.x,r.y,r.w,r.h)}else if(m==='blur'){target.filter=`blur(${Math.max(4,k/2)}px)`;target.drawImage(base,r.x,r.y,r.w,r.h,r.x,r.y,r.w,r.h);target.filter='none'}else{const tw=Math.max(1,Math.round(r.w/Math.max(6,k))),th=Math.max(1,Math.round(r.h/Math.max(6,k))),t=document.createElement('canvas');t.width=tw;t.height=th;const tc=t.getContext('2d');tc.imageSmoothingEnabled=true;tc.drawImage(base,r.x,r.y,r.w,r.h,0,0,tw,th);target.imageSmoothingEnabled=false;target.drawImage(t,0,0,tw,th,r.x,r.y,r.w,r.h);target.imageSmoothingEnabled=true}target.restore()}
async function autoDetect(){if(!S.file)return;let added=0;if($('#plateOpt').checked){S.regions.push({x:clean.width*.35,y:clean.height*.68,w:clean.width*.3,h:clean.height*.095,source:'auto',label:'번호판 후보'});added++}if($('#faceOpt').checked&&'FaceDetector'in window){try{const detector=new FaceDetector({fastMode:true,maxDetectedFaces:10}),faces=await detector.detect(clean);faces.forEach(f=>{const b=f.boundingBox;S.regions.push({x:b.x,y:b.y,w:b.width,h:b.height,source:'auto',label:'얼굴 후보'});added++})}catch{toast('이 브라우저의 얼굴 탐지 기능을 사용할 수 없습니다.','warn')}}else if($('#faceOpt').checked)toast('얼굴 자동 탐지는 지원 브라우저에서만 동작합니다. 수동 영역은 모든 브라우저에서 사용할 수 있습니다.','warn');drawBase();toast(`${added}개 자동 후보를 추가했습니다.`)}
async function sha(blob){const b=await blob.arrayBuffer(),d=await crypto.subtle.digest('SHA-256',b);return[...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function openDB(){return new Promise((res,rej)=>{const q=indexedDB.open(DB,1);q.onupgradeneeded=()=>STORES.forEach(n=>{if(!q.result.objectStoreNames.contains(n))q.result.createObjectStore(n)});q.onsuccess=()=>res(q.result);q.onerror=()=>rej(q.error)})}
function txPut(db,store,key,val){return new Promise((res,rej)=>{const t=db.transaction(store,'readwrite'),q=t.objectStore(store).put(val,key);q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})}
async function distributed(blob,id){const db=await openDB(),n=Math.ceil(blob.size/CHUNK);for(let i=0;i<n;i++){const data=await blob.slice(i*CHUNK,Math.min(blob.size,(i+1)*CHUNK)).arrayBuffer(),pairs=[[0,1],[1,2],[2,0]][i%3];for(const p of pairs)await txPut(db,STORES[p],`${id}:${i}`,{job:id,index:i,size:data.byteLength,data,created:Date.now()});progress(84+(i+1)/n*10,`분할 청크 ${i+1}/${n} 복제 저장 중`)}db.close();return n*2}
async function storageStats(){try{const db=await openDB(),out=[];for(const store of STORES)out.push(await new Promise((res,rej)=>{let size=0,count=0;const t=db.transaction(store,'readonly'),q=t.objectStore(store).openCursor();q.onsuccess=e=>{const c=e.target.result;if(c){size+=c.value.size||0;count++;c.continue()}else res({size,count})};q.onerror=()=>rej(q.error)}));db.close();const max=Math.max(1,...out.map(x=>x.size));els.nodes.innerHTML=out.map((x,i)=>`<div class="node"><div class="nodeTop"><b>Node ${String.fromCharCode(65+i)}</b><span>${bytes(x.size)} · ${x.count} 청크</span></div><div class="nodeTrack"><i style="width:${x.size/max*100}%"></i></div></div>`).join('');els.storageTotal.textContent=bytes(out.reduce((a,b)=>a+b.size,0))}catch{els.nodes.innerHTML='<div class="empty">저장소를 확인할 수 없습니다.</div>'}}
async function processImage(){const out=document.createElement('canvas');out.width=clean.width;out.height=clean.height;const oc=out.getContext('2d');oc.drawImage(clean,0,0);S.regions.forEach(r=>mask(oc,clean,r));progress(65,'이미지 마스킹을 적용했습니다.');const type=$('#format').value;return await new Promise(res=>out.toBlob(res,type,type==='image/jpeg'?.92:undefined))}
async function processVideo(){if(!window.MediaRecorder||!HTMLCanvasElement.prototype.captureStream)throw new Error('이 브라우저는 영상 출력을 지원하지 않습니다. Chrome 또는 Edge 최신 버전을 사용하세요.');const out=document.createElement('canvas');out.width=clean.width;out.height=clean.height;const oc=out.getContext('2d'),stream=out.captureStream(30),srcCap=els.video.captureStream?.()||els.video.mozCaptureStream?.();srcCap?.getAudioTracks().forEach(t=>stream.addTrack(t));const types=['video/webm;codecs=vp9,opus','video/webm;codecs=vp8,opus','video/webm'];const mime=types.find(MediaRecorder.isTypeSupported)||'';const rec=new MediaRecorder(stream,mime?{mimeType:mime}:undefined),parts=[];rec.ondataavailable=e=>{if(e.data.size)parts.push(e.data)};const stopped=new Promise((res,rej)=>{rec.onstop=()=>res(new Blob(parts,{type:rec.mimeType||'video/webm'}));rec.onerror=()=>rej(rec.error||new Error('영상 인코딩 오류'))});els.video.pause();els.video.currentTime=0;await new Promise(res=>{els.video.onseeked=res;setTimeout(res,500)});rec.start(500);await els.video.play();await new Promise(resolve=>{const draw=()=>{oc.clearRect(0,0,out.width,out.height);oc.drawImage(els.video,0,0,out.width,out.height);S.regions.forEach(r=>mask(oc,out,r));const p=els.video.duration?20+els.video.currentTime/els.video.duration*58:40;progress(p,`영상 프레임 처리 중 · ${clock(els.video.currentTime)} / ${clock(els.video.duration)}`);if(els.video.ended||els.video.paused&&els.video.currentTime>=els.video.duration-.05)return resolve();requestAnimationFrame(draw)};draw();els.video.onended=resolve});if(rec.state!=='inactive')rec.stop();els.video.pause();return await stopped}
async function run(){if(!S.file||S.processing)return;S.processing=true;els.process.disabled=true;try{progress(5,'파일과 처리 옵션을 준비하고 있습니다.');await new Promise(r=>setTimeout(r,150));if(!S.regions.length){await autoDetect();if(!S.regions.length)throw new Error('비식별화할 영역을 하나 이상 지정하세요.')}progress(15,'마스킹 영역을 확인했습니다.');const blob=S.kind==='image'?await processImage():await processVideo();if(!blob)throw new Error('결과 파일을 만들지 못했습니다.');progress(78,'SHA-256 무결성 해시 생성 중');const digest=await sha(blob),id=uid(),chunks=$('#storeOpt').checked?await distributed(blob,id):0;progress(96,'결과 화면을 준비하고 있습니다.');S.result=blob;S.resultUrl=URL.createObjectURL(blob);S.manifest={job_id: