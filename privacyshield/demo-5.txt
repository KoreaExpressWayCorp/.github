id,created_at:new Date().toISOString(),source:{name:S.file.name,type:S.file.type,size:S.file.size},output:{type:blob.type,size:blob.size,sha256:digest},regions:S.regions.map(r=>({x:Math.round(r.x),y:Math.round(r.y),width:Math.round(r.w),height:Math.round(r.h),source:r.source,label:r.label})),storage:{engine:'IndexedDB browser simulation',chunk_size:CHUNK,replication_factor:2,replica_chunks:chunks}};showResult();S.history.unshift({id,name:S.file.name,at:new Date().toISOString(),regions:S.regions.length,size:blob.size});saveHistory();renderHistory();await storageStats();progress(100,'비식별화 처리가 완료되었습니다.');toast('처리가 완료되었습니다. 결과를 다운로드하세요.');els.result.scrollIntoView({behavior:'smooth'})}catch(e){toast(e.message||'처리 중 오류가 발생했습니다.','err');progress(0,'작업이 중단되었습니다.')}finally{S.processing=false;els.process.disabled=!S.file}}
function showResult(){els.result.style.display='block';els.jobId.textContent=S.manifest.job_id;els.mRegions.textContent=S.regions.length+'개';els.mSize.textContent=bytes(S.result.size);els.mChunks.textContent=S.manifest.storage.replica_chunks+'개';els.mType.textContent=S.result.type||'파일';els.hash.textContent=S.manifest.output.sha256;if(S.kind==='image'){els.resultImg.src=S.resultUrl;els.resultImg.style.display='block';els.resultVideo.style.display='none'}else{els.resultVideo.src=S.resultUrl;els.resultVideo.style.display='block';els.resultImg.style.display='none'}}
function download(blob,name){const a=document.createElement('a'),u=URL.createObjectURL(blob);a.href=u;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),1500)}
function renderHistory(){if(!S.history.length){els.history.innerHTML='<tr><td class="empty" colspan="4">아직 처리한 파일이 없습니다.</td></tr>';return}els.history.innerHTML=S.history.map(x=>`<tr><td>${escapeHTML(x.name)}</td><td>${new Date(x.at).toLocaleString('ko-KR')}</td><td>${x.regions}개</td><td>${bytes(x.size)}</td></tr>`).join('')}
function escapeHTML(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function clearStore(){const db=await openDB();await Promise.all(STORES.map(n=>new Promise((res,rej)=>{const q=db.transaction(n,'readwrite').objectStore(n).clear();q.onsuccess=res;q.onerror=()=>rej(q.error)})));db.close();await storageStats();toast('브라우저 분할 저장소를 비웠습니다.')}
function demo(){const c=document.createElement('canvas');c.width=1280;c.height=720;const x=c.getContext('2d');const g=x.createLinearGradient(0,0,0,720);g.addColorStop(0,'#6fa6c7');g.addColorStop(.45,'#bfd0d8');g.addColorStop(.46,'#485a64');g.addColorStop(1,'#1d2830');x.fillStyle=g;x.fillRect(0,0,1280,720);x.fillStyle='#d7e2e7';x.fillRect(0,520,1280,8);x.fillStyle='#243c50';x.fillRect(330,300,650,260);x.fillStyle='#355d78';x.beginPath();x.moveTo(430,300);x.lineTo(535,210);x.lineTo(805,210);x.lineTo(900,300);x.fill();x.fillStyle='#101820';x.beginPath();x.arc(455,560,70,0,Math.PI*2);x.arc(855,560,70,0,Math.PI*2);x.fill();x.fillStyle='#fff';x.fillRect(585,470,170,58);x.fillStyle='#111';x.font='bold 38px sans-serif';x.textAlign='center';x.fillText('12가 3456',670,512);x.fillStyle='#f0b89c';x.beginPath();x.arc(585,260,34,0,Math.PI*2);x.arc(750,260,34,0,Math.PI*2);x.fill();c.toBlob(b=>useFile(new File([b],'privacyshield-demo.png',{type:'image/png'})),'image/png')}
els.file.onchange=e=>useFile(e.target.files[0]);['dragenter','dragover'].forEach(n=>els.drop.addEventListener(n,e=>{e.preventDefault();els.drop.classList.add('drag')}));['dragleave','drop'].forEach(n=>els.drop.addEventListener(n,e=>{e.preventDefault();els.drop.classList.remove('drag')}));els.drop.addEventListener('drop',e=>useFile(e.dataTransfer.files[0]));els.canvas.onpointerdown=e=>{if(!S.file)return;els.canvas.setPointerCapture(e.pointerId);S.start=point(e);S.draft={...normalize(S.start,S.start)};drawBase()};els.canvas.onpointermove=e=>{if(!S.start)return;S.draft=normalize(S.start,point(e));drawBase()};els.canvas.onpointerup=e=>{if(!S.start)return;const r=normalize(S.start,point(e));S.start=null;S.draft=null;if(r.w>8&&r.h>8)S.regions.push(r);drawBase()};$('#autoBtn').onclick=autoDetect;$('#undoBtn').onclick=()=>{S.regions.pop();drawBase()};$('#clearBtn').onclick=()=>{S.regions=[];drawBase()};$('#removeBtn').onclick=()=>reset();$('#strength').oninput=e=>$('#strengthOut').value=e.target.value;els.process.onclick=run;els.play.onclick=async()=>{if(els.video.paused){await els.video.play();els.play.textContent='Ⅱ';frame()}else{els.video.pause();els.play.textContent='▶'}};els.seek.oninput=e=>{if(els.video.duration)els.video.currentTime=e.target.value/1000*els.video.duration};els.video.onseeked=frame;els.video.onpause=()=>els.play.textContent='▶';els.download.onclick=()=>{if(!S.result)return;const ext=S.kind==='video'?'webm':($('#format').value.split('/')[1].replace('jpeg','jpg'));download(S.result,`${S.file.name.replace(/\.[^.]+$/,'')}-anonymized.${ext}`)};els.manifest.onclick=()=>S.manifest&&download(new Blob([JSON.stringify(S.manifest,null,2)],{type:'application/json'}),`${S.manifest.job_id}-manifest.json`);$('#newBtn').onclick=()=>reset();$('#demoBtn').onclick=demo;$('#historyClear').onclick=()=>{S.history=[];saveHistory();renderHistory();toast('작업 이력을 삭제했습니다.')};$('#storageClear').onclick=clearStore;renderHistory();storageStats();
})();
</script>
</body>
</html>
